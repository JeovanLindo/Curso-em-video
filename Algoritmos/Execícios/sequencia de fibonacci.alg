algoritmo "EXERCICIO"

var
    d, c, p : inteiro
inicio
     d <- 1
     p <- 1
       escreva (0)
     para c <- 1 ate 7 faca
       escreva (p, d)
       p <- (d + p)
       d <- (p + d)
     fimpara
     
fimalgoritmo
